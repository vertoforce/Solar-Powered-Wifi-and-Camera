#include "tb.h"

// This file controls the pins to control the AP and camera
#define AP_PIN 14
#define CAMERA_PIN 12

void setup_pins() {
    pinMode(AP_PIN, OUTPUT);
    pinMode(CAMERA_PIN, OUTPUT);
}

void update_on_off() {
    // Get attributes

    // Turn on/off pins that need to be changed
 
}